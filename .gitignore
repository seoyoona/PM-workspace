# Real client data — do not commit
clients/*/
!clients/CLAUDE.md
glossary/*.md
!glossary/example-client.md
handoffs/docx_images/
handoffs/*.docx

# Local settings (per-user permissions, paths)
.claude/settings.local.json

# Teams webhook/flow URLs (sensitive)
.env.teams

# OS files
.DS_Store
